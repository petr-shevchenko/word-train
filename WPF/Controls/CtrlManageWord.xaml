<UserControl x:Class="WordTrain.CtrlManageWord"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1100" FontSize="16">
    <Grid KeyDown="Grid_KeyDown">
    <DockPanel LastChildFill="True">
        <Expander x:Name="expander" DockPanel.Dock="Bottom" Header="Поштучное добавление слов" Expanded="Expander_Expanded" FontWeight="DemiBold">
                <Grid x:Name="expandGrid" Height="70">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" />
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" Grid.Row="0" Text="Eng (изучаемое слово)" HorizontalAlignment="Center" VerticalAlignment="Center" />
                   
                    <TextBlock Grid.Column="1" Grid.Row="0" Text="Rus (первод)" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Grid.Row="0" Text="Тема" HorizontalAlignment="Center" VerticalAlignment="Center" />

                    <GridSplitter Grid.Row="0" Grid.ColumnSpan="3" HorizontalAlignment="Stretch"  Height="1" VerticalAlignment="Bottom" Background="Black" />
                    <GridSplitter Grid.Column="0" Grid.RowSpan="2" VerticalAlignment="Stretch"  Width="1" HorizontalAlignment="Right" Background="Black" />
                    <GridSplitter Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Stretch"  Width="1" HorizontalAlignment="Right" Background="Black" />
                    <GridSplitter Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Stretch"  Width="1" HorizontalAlignment="Right" Background="Black" />

                    <TextBox x:Name="tbEng" Grid.Column="0" Grid.Row="1" Text="" HorizontalAlignment="Stretch" Margin="5,2,5,2"/>
                    <TextBox x:Name="tbRus" Grid.Column="1" Grid.Row="1" Text="" HorizontalAlignment="Stretch" Margin="5,2,5,2"/>
                    <TextBox x:Name="tbTheme" Grid.Column="2" Grid.Row="1" Text="" HorizontalAlignment="Stretch" Margin="5,2,5,2"/>
                    
                    <Button Grid.Column="3" Grid.RowSpan="2" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" x:Name="AddWord" ToolTip="Добавить слово" 
                            Click="AddWord_Click" Content="{DynamicResource Add}" Margin="10" Width="40" Height="40" />
                </Grid>
            </Expander>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="50">
               <Button x:Name="AddLine" ToolTip="Добавить строку" Click="AddLine_Click" Content="{DynamicResource Add}" Margin="5" Width="40" />
                <Button x:Name="DelLine" ToolTip="Удалить выделенные строки" Click="DelLine_Click" Content="{DynamicResource Del}" Margin="5" Width="40"/>
                <Separator Width="10" Opacity="0"/>
                <Button x:Name="Save" ToolTip="Сохранить данные в файл" Click="Save_Click" Content="{DynamicResource Save}" Margin="5" Width="40" />
                <Button x:Name="ResetRepeat" ToolTip="Сбросить повторы" Click="ResetRepeat_Click" Content="{DynamicResource ResetRepeat}" Margin="5" Width="40" />
                <Separator Width="20" Opacity="0"/>
                <TextBox x:Name="tbInputTetx" Width="150" Margin="5" VerticalAlignment="Center" VerticalContentAlignment="Center" Height="40"/>
                <Button x:Name="btnSetText" Click="btnSetText_Click"  Content="Задать значение" Margin="5" Width="140"/>
                <Separator Width="20" Opacity="0"/>
                <TextBox x:Name="tbFilter" Width="150" Margin="5" VerticalAlignment="Center" TextChanged="tbFilter_TextChanged" VerticalContentAlignment="Center" Height="40"/>
                <Button x:Name="btnFilter" ToolTip="Фильтровать слова по выбранному критерию" Click="Filter_Click"  Content="{DynamicResource Filter}" Margin="5" Width="40"/>
                <RadioButton x:Name="rbEng" Content="Eng"  VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="5" IsChecked="True"/>
                <RadioButton x:Name="rbRus" Content="Rus"  VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="5"/>
                <RadioButton x:Name="rbTheme" Content="Theme"  VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="5"/>
                <CheckBox x:Name="cbAddSimilar" Content="+Похожие" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="5" IsChecked="False"/>
            </StackPanel>
            <DataGrid x:Name="dgWordList" Margin="5" IsReadOnly="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" AutoGenerateColumns="False" HorizontalScrollBarVisibility="Auto"
                  CanUserReorderColumns="False" CanUserResizeColumns="True" SelectionMode="Extended" SelectionUnit="Cell">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem x:Name="ContextMenuMoveToExcept" Header="Переместить в исключения" FontWeight="Bold"  Icon="{DynamicResource Replace1}" Click="ContextMenuMoveToExcept_Click"/>
                        <MenuItem Header="Удалить выбранные" Click="DelLine_Click" FontWeight="Bold" Icon="{DynamicResource Del}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="70" Binding="{Binding ID}" />
                    <DataGridTextColumn Header="English" Width="150" Binding="{Binding Eng}"/>
                    <DataGridTextColumn Header="Russian" Width="500" Binding="{Binding Rus}"/>
                    <DataGridTextColumn Header="Theme" Width="150" Binding="{Binding Theme}"/>
                    <DataGridTextColumn Header="Repeat" Width="60" Binding="{Binding Repeat}"/>
                    <DataGridTextColumn Header="Errors" Width="60" Binding="{Binding Errors}"/>
                    <DataGridTextColumn Header="Count" Width="60" Binding="{Binding Count}"/>
                    <DataGridTextColumn Header="№№" Width="60" Binding="{Binding N}"/>
                    <DataGridTextColumn Header="TPF" Width="*" Binding="{Binding TempRepeatFlag}"/>
                </DataGrid.Columns>

        </DataGrid>

        
        
    </DockPanel>
       
    </Grid>
</UserControl>
